name: Setup
description: Checkout, install uv, set up Python, and install dependencies

runs:
  using: composite
  steps:
    - name: Install uv
      uses: astral-sh/setup-uv@fac544c07dec837d0ccb6301d7b5580bf5edae39  # v8.2.0
      with:
        enable-cache: true
        cache-python: true

    - name: Set up Python
      run: uv python install 3.14
      shell: bash

    - name: Install dependencies
      run: uv sync --locked --extra dev
      shell: bash
      env:
        UV_MALWARE_CHECK: "1"
