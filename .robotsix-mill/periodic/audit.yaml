# Per-repo periodic workflow: presence enables audit for this repo.
# Baseline codebase-health / tooling-coverage review that proposes
# worthwhile improvements and files draft tickets. Repo-agnostic — this
# file partial-merges over the built-in audit definition (lives in
# robotsix-mill: agent_definitions/periodic/audit.yaml).
#
# robotsix-yaml-config is a confirmed live, consumed library (shared by
# robotsix-mill and robotsix-auto-mail), so it receives the same
# baseline periodic review as sibling library robotsix-modules.
name: audit
