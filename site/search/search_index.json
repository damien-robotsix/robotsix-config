{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"robotsix-yaml-config","text":"<p>Backend-agnostic primitives for a layered YAML configuration cascade:</p> <ol> <li>Start from code-defined defaults.</li> <li>Deep-merge one or more YAML config files in precedence order.</li> <li>Overlay environment variables field-by-field (highest priority).</li> <li>Flatten the nested dict into flat keyword arguments via a    dotted-path alias map.</li> </ol> <p>The package operates on plain dicts only \u2014 no pydantic, no pydantic-settings \u2014 so it can be consumed by any configuration backend.</p>"},{"location":"#installation","title":"Installation","text":"<p>This package is published to PyPI:</p> <pre><code># Using pip\npip install robotsix-yaml-config\n\n# Using uv (recommended for development)\nuv add robotsix-yaml-config\n</code></pre> <p>Python version: requires Python 3.14 or later.</p>"},{"location":"#quick-start","title":"Quick start","text":"<p>The four pipeline stages \u2014 code defaults, deep-merged YAML files, env overlay, and flatten \u2014 compose like this:</p> <pre><code>from pathlib import Path\nfrom robotsix_yaml_config import (\n    deep_merge,\n    load_yaml_cascade,\n    overlay_env_vars,\n    flatten_config,\n)\n\n# 1. Code-defined defaults.\nconfig = {\"host\": \"localhost\", \"port\": 5432, \"debug\": False}\n\n# 2. Deep-merge YAML files in precedence order (later layers win).\n#    Each layer is a (path, required) tuple: required-but-missing -&gt; YamlConfigError;\n#    optional-but-missing -&gt; skipped.\nfile_config = load_yaml_cascade([\n    (Path(\"defaults.yaml\"), False),  # optional layer\n    (Path(\"config.yaml\"), True),     # required layer\n])\ndeep_merge(config, file_config)      # mutates `config` in place, returns it\n\n# 3. Overlay env vars onto existing top-level keys (highest priority).\n#    Reads APP_HOST / APP_PORT / APP_DEBUG; coerces with type_hints.\n#    e.g. `export APP_PORT=6543` -&gt; config[\"port\"] == 6543 (int)\noverlay_env_vars(config, prefix=\"APP\", type_hints={\"port\": int, \"debug\": bool})\n\n# 4. Flatten into keyword arguments via a dotted-path alias map.\nflat = flatten_config(config, alias_map={\"host\": \"db_host\", \"port\": \"db_port\"})\n# flat == {\"db_host\": \"localhost\", \"db_port\": 6543}\n</code></pre> <p>See the API Reference for full documentation of every public symbol.</p>"},{"location":"api/","title":"API Reference","text":""},{"location":"contributing/","title":"Contributing","text":"<p>See CONTRIBUTING.md in the repository for the full contributor workflow.</p>"},{"location":"_core/","title":"Core","text":"<p>Core YAML cascade primitives: deep-merge, file reading, layered load.</p> <p>Ported from the duplicated implementations in <code>robotsix-mill</code> and <code>robotsix-auto-mail</code> so both repos consume one source of truth.</p>"},{"location":"_core/#robotsix_yaml_config._core.deep_merge","title":"<code>deep_merge(base, overlay)</code>","text":"<p>Recursively merge overlay into base (mutates base).</p> <p>For each key in overlay: if it already exists in base and both values are dicts, recurse; otherwise <code>base[key]</code> is replaced with a <code>deepcopy</code> of the overlay value.  Lists and other non-dict values are replaced wholesale (never extended).  Returns base.</p> Source code in <code>src/robotsix_yaml_config/_core.py</code> <pre><code>def deep_merge(base: dict[str, Any], overlay: dict[str, Any]) -&gt; dict[str, Any]:\n    \"\"\"Recursively merge *overlay* into *base* (mutates *base*).\n\n    For each key in *overlay*: if it already exists in *base* and both\n    values are dicts, recurse; otherwise ``base[key]`` is replaced with\n    a ``deepcopy`` of the overlay value.  Lists and other non-dict\n    values are replaced wholesale (never extended).  Returns *base*.\n    \"\"\"\n    for key, value in overlay.items():\n        if key in base and isinstance(base[key], dict) and isinstance(value, dict):\n            deep_merge(base[key], value)\n        else:\n            base[key] = deepcopy(value)\n    return base\n</code></pre>"},{"location":"_core/#robotsix_yaml_config._core.load_yaml_cascade","title":"<code>load_yaml_cascade(layers)</code>","text":"<p>Load and deep-merge YAML files in order; later layers win.</p> <p>Each layer is a <code>(path, required)</code> tuple.  A required layer whose file is missing raises <code>MissingConfigError</code> (a subclass of <code>YamlConfigError</code>) naming the file.  Optional layers whose files are missing are skipped.  Returns the fully merged dict (starting from an empty dict).</p> Source code in <code>src/robotsix_yaml_config/_core.py</code> <pre><code>def load_yaml_cascade(layers: Sequence[tuple[Path, bool]]) -&gt; dict[str, Any]:\n    \"\"\"Load and deep-merge YAML files in order; later layers win.\n\n    Each layer is a ``(path, required)`` tuple.  A required layer whose\n    file is missing raises ``MissingConfigError`` (a subclass of\n    ``YamlConfigError``) naming the file.  Optional\n    layers whose files are missing are skipped.  Returns the fully merged\n    dict (starting from an empty dict).\n    \"\"\"\n    merged: dict[str, Any] = {}\n    for path, required in layers:\n        if not path.exists():\n            if required:\n                raise MissingConfigError(f\"Required config file not found: {path}.\")\n            continue\n        deep_merge(merged, read_yaml_file(path))\n    return merged\n</code></pre>"},{"location":"_core/#robotsix_yaml_config._core.read_yaml_file","title":"<code>read_yaml_file(path)</code>","text":"<p>Read and parse a single YAML file, returning a dict.</p> <p>Returns an empty dict for non-existent files.  Raises <code>YamlReadError</code> on OS-level read failures, <code>YamlParseError</code> on parse failures, and <code>InvalidConfigStructureError</code> for a non-dict top level (all subclasses of <code>YamlConfigError</code>).</p> Source code in <code>src/robotsix_yaml_config/_core.py</code> <pre><code>def read_yaml_file(path: Path) -&gt; dict[str, Any]:\n    \"\"\"Read and parse a single YAML file, returning a dict.\n\n    Returns an empty dict for non-existent files.  Raises\n    ``YamlReadError`` on OS-level read failures, ``YamlParseError`` on\n    parse failures, and ``InvalidConfigStructureError`` for a non-dict\n    top level (all subclasses of ``YamlConfigError``).\n    \"\"\"\n    if not path.exists():\n        return {}\n    try:\n        with open(path, encoding=\"utf-8\") as fh:\n            data = yaml.safe_load(fh)\n    except OSError as exc:\n        raise YamlReadError(f\"Failed to read {path}: {exc}\") from exc\n    except yaml.YAMLError as exc:\n        raise YamlParseError(f\"YAML parse error in {path}: {exc}\") from exc\n    if data is None:\n        return {}\n    if not isinstance(data, dict):\n        raise InvalidConfigStructureError(\n            f\"Expected a mapping at top level of {path}, got {type(data).__name__}\"\n        )\n    return data\n</code></pre>"},{"location":"_env/","title":"Environment","text":"<p>Overlay environment variables onto a flat config dict.</p>"},{"location":"_env/#robotsix_yaml_config._env.overlay_env_vars","title":"<code>overlay_env_vars(config, prefix, type_hints=None)</code>","text":"<p>Overlay <code>{PREFIX}_{KEY.upper()}</code> env vars onto config.</p> <p>For each key already present in config, look up the env var named <code>f\"{prefix}_{key.upper()}\"</code>.  If it is set, coerce its string value using <code>type_hints.get(key)</code> (defaulting to <code>str</code> when absent or when type_hints is <code>None</code>) and overwrite <code>config[key]</code>.  Keys that are not already in config are never added.  Mutates and returns config.</p> Source code in <code>src/robotsix_yaml_config/_env.py</code> <pre><code>def overlay_env_vars(\n    config: dict[str, Any],\n    prefix: str,\n    type_hints: dict[str, type] | None = None,\n) -&gt; dict[str, Any]:\n    \"\"\"Overlay ``{PREFIX}_{KEY.upper()}`` env vars onto *config*.\n\n    For each key already present in *config*, look up the env var named\n    ``f\"{prefix}_{key.upper()}\"``.  If it is set, coerce its string value\n    using ``type_hints.get(key)`` (defaulting to ``str`` when absent or\n    when *type_hints* is ``None``) and overwrite ``config[key]``.  Keys\n    that are not already in *config* are never added.  Mutates and\n    returns *config*.\n    \"\"\"\n    hints = type_hints or {}\n    for key in config:\n        env_name = f\"{prefix}_{key.upper()}\"\n        if env_name in os.environ:\n            hint = hints.get(key, str)\n            config[key] = _coerce(os.environ[env_name], hint)\n    return config\n</code></pre>"},{"location":"_errors/","title":"Errors","text":"<p>Error types for the YAML configuration cascade.</p>"},{"location":"_errors/#robotsix_yaml_config._errors.InvalidConfigStructureError","title":"<code>InvalidConfigStructureError</code>","text":"<p>               Bases: <code>YamlConfigError</code></p> <p>The YAML top level is not a mapping.</p>"},{"location":"_errors/#robotsix_yaml_config._errors.MissingConfigError","title":"<code>MissingConfigError</code>","text":"<p>               Bases: <code>YamlConfigError</code>, <code>FileNotFoundError</code></p> <p>A required config file was not found.</p>"},{"location":"_errors/#robotsix_yaml_config._errors.YamlConfigError","title":"<code>YamlConfigError</code>","text":"<p>               Bases: <code>Exception</code></p> <p>Base for all YAML-config cascade failures.</p>"},{"location":"_errors/#robotsix_yaml_config._errors.YamlParseError","title":"<code>YamlParseError</code>","text":"<p>               Bases: <code>YamlConfigError</code></p> <p>The file is not valid YAML.</p>"},{"location":"_errors/#robotsix_yaml_config._errors.YamlReadError","title":"<code>YamlReadError</code>","text":"<p>               Bases: <code>YamlConfigError</code></p> <p>An OS-level error occurred while reading a config file.</p>"},{"location":"_flatten/","title":"Flatten","text":"<p>Flatten a nested config dict into <code>{alias: value}</code> via a path map.</p>"},{"location":"_flatten/#robotsix_yaml_config._flatten.flatten_config","title":"<code>flatten_config(nested, alias_map)</code>","text":"<p>Flatten nested into a flat <code>{alias: value}</code> dict.</p> <p>Walks nested building dotted-path keys (<code>\"a.b.c\"</code>).  For each path, look it up in alias_map; if found, emit <code>result[alias] = value</code> AS-IS \u2014 including dict-valued aliases \u2014 and stop descending that branch.  If the path is not mapped and the value is a dict, recurse.  Unknown leaf paths are silently dropped.  When the same alias is reachable via multiple paths, the last-traversed wins (dict insertion order).</p> Source code in <code>src/robotsix_yaml_config/_flatten.py</code> <pre><code>def flatten_config(nested: dict[str, Any], alias_map: dict[str, str]) -&gt; dict[str, Any]:\n    \"\"\"Flatten *nested* into a flat ``{alias: value}`` dict.\n\n    Walks *nested* building dotted-path keys (``\"a.b.c\"``).  For each\n    path, look it up in *alias_map*; if found, emit ``result[alias] =\n    value`` AS-IS \u2014 including dict-valued aliases \u2014 and stop descending\n    that branch.  If the path is not mapped and the value is a dict,\n    recurse.  Unknown leaf paths are silently dropped.  When the same\n    alias is reachable via multiple paths, the last-traversed wins\n    (dict insertion order).\n    \"\"\"\n    result: dict[str, Any] = {}\n\n    def _walk(d: dict[str, Any], prefix: str = \"\") -&gt; None:\n        for key, value in d.items():\n            full_key = f\"{prefix}.{key}\" if prefix else key\n            alias = alias_map.get(full_key)\n            if alias is not None:\n                result[alias] = value\n                continue\n            if isinstance(value, dict):\n                _walk(value, full_key)\n\n    _walk(nested)\n    return result\n</code></pre>"}]}